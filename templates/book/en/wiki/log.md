# Log

Chronological, append-only: one entry per ingest, query or lint, newest at the bottom.
Every entry starts with `## [YYYY-MM-DD] operation | title` so `grep "^## \["` works.

## [2026-09-04] init | Applied the "Reading" template
